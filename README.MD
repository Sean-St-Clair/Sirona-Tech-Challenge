The instructions to run the project are simple. Make sure you have node installed and the following
dependencies:
- express
- http
- path

You can host the web server locally by executing the command: node server.js and visiting http://localhost:3000/ (or any other environment-set port)

If you would like to test some of the cases, or author some PUT / POST requests, feel free to make
use of the provided test.js file, which demonstrates all of the required functionality.
